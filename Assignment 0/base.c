#include <stdio.h>

int main() {
    float base = 10.0;     
    float height = 5.0;    
    float area;

    area = (base * height) / 2;

    printf("Area of Triangle = %.2f\n", area);

    return 0;
}